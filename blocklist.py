# from models import UserModel
# from sqlalchemy.orm import Session

# session = Session()

# query = session.query(UserModel.jwt).all()

# BLOCKLIST = set(result.jwt for result in query)

BLOCKLIST = set()
